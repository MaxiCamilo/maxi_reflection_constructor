name: maxi_reflection_constructor
description: Separate library from the reflectors; it is used to examine a project, detect classes, and build the reflectors. Priority is given to keeping the generated structure constant and static.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

  meta: ^1.17.0
  analyzer: ^8.4.1
  # path: ^1.9.0
  maxi_framework:  
    path: ../maxi_framework

  maxi_reflection:
    path: ../maxi_reflection

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
